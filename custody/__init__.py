"""Chain of Custody — renderer and ledger for the alanhasn profile.

Only `config` and `ledger` are imported by `verify.py`; both are stdlib-only so
anyone can audit the chain without installing the build toolchain.
"""
